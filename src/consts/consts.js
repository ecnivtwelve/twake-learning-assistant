import flag from 'cozy-flags'

export const OPENRAG_URL = flag('learnings.openrag.url')
export const AUTH_TOKEN = flag('learnings.openrag.token')
export const LUCIE_URL = flag('learnings.lucie.url')
export const LUCIE_API_KEY = flag('learnings.lucie.token')
